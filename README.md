# MediaInfo Docker Image with libcurl

Image created be able to run mediainfo with URL from S3 buckets. 
Compiled using --with-libcurl

## Usage

`docker run alexrex/mediainfo`

Example:

`docker run alexrex/mediainfo --version`
